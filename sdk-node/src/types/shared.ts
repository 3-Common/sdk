/**
 * Cross-resource types used by more than one resource folder.
 *
 * Empty for now (only one resource exists). Populated when a second resource
 * lands and we need shared envelopes / pagination metadata types.
 *
 * @public
 */

export {}
