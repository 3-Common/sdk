# Changesets

Run `npx changeset` to record a user-facing change. Each changeset becomes a CHANGELOG entry on the next release.

For internal changes that don't need a release entry (e.g. test refactors), skip the changeset.
