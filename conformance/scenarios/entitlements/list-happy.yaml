name: list entitlements — happy path
call:
  resource: entitlements
  method: list
  args:
    featureKey: api_calls
    minBalance: 1
    pageSize: 10
expectedRequest:
  method: GET
  path: /v1/entitlements
  query:
    featureKey: api_calls
    minBalance: '1'
    pageSize: '10'
  headers:
    authorization: 'Bearer 3co_test'
    threecommon-version: '2026-04-29'
mockResponse:
  status: 200
  headers:
    x-request-id: req-ent-list-001
  body:
    data:
      - id: ent_a
        hostId: hst_1
        contactId: cnt_42
        featureKey: api_calls
        balance: 100
        totalGranted: 100
        totalConsumed: 0
        createdAt: '2026-05-01T00:00:00.000Z'
        updatedAt: '2026-05-01T00:00:00.000Z'
    hasMore: false
expectedResult:
  data:
    - id: ent_a
      featureKey: api_calls
      balance: 100
  hasMore: false
